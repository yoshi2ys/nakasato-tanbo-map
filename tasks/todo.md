# 2026-08-14 — 田んぼ輪郭描画ツール 実装計画

航空写真上に田んぼの輪郭を手動・自動で描き、面積を表示するブラウザツール。ローカル実行のみ、デプロイなし。

## 技術選定

- **地図**: MapLibre GL JS + 写真タイル 2 枚重ね
  - 上: 十日町市公開地理情報システムの航空写真（2022 年撮影）
    `https://geogeo.blob.core.windows.net/tiles/15210/2022/ortho/{z}/{x}/{y}.png`
    **z20 まで**（約 12cm/px）あり、地理院タイルの 4 倍の解像度。同一 z18 タイルで 150KB 対 59.5KB。
    y 軸が反転した **TMS 方式**なので `scheme: 'tms'` が必要。配信範囲は市域のみで、`bounds` に実測した外接矩形 `[138.5156, 36.8093, 138.9551, 37.3003]`（z12 タイルを総当たりして算出）を渡す。
    著作権は十日町市にあり、**私的使用の範囲内でのみ利用可**（営利・商業利用は不可）。出典表記を付けている。
  - 下: 国土地理院「電子国土基本図（オルソ画像）」`https://cyberjapandata.gsi.go.jp/xyz/ort/{z}/{x}/{y}.jpg`（z18 まで、API キー不要、出典表記必須）
    「全国最新写真（シームレス）」より鮮明で、同一 z18 タイルで 15.7KB に対して 59.5KB。市外ではこちらが見える。
  - z19 以上は国土地理院に存在しない（404）。Esri World Imagery もこの地域の z19 以上は「データなし」。
  - Google Maps は頂点編集の自由度が低いため不採用。
  - ズーム 17 を超えると必ず引き伸ばしになるので、`raster-resampling: 'nearest'` にして既定の平滑化を切っている。
- **面積計算**: Turf.js `turf.area()`（㎡ → 反・畝換算も表示）
- **描画・頂点編集**: 自前実装(mapbox-gl-draw は MapLibre 対応が不安定、頂点増減 UI も要件と合わない）
- **自動検出**: OpenCV.js（WASM）をブラウザ内で実行
- **ビルド**: Vite + vanilla TS。`npm run dev` でローカル起動

## Plan

- [x] **Step 1 — 地図表示**: Vite + vanilla TS で MapLibre に国土地理院写真タイルを表示、出典クレジット付与。
      → verify: `npm run dev` でブラウザに航空写真が表示される。
- [x] **Step 2 — 手動ポリゴン描画**: クリックで頂点追加（GeoJSON ソース + レイヤー）。開始点から 12px 以内のクリックでスナップ閉合（`map.project()` でスクリーン座標比較）。ダブルクリック / Enter でも閉合。閉合時に `turf.area()` で面積を㎡・反・畝表示。
      → verify: 実際に輪郭を描いて閉じ、面積が妥当な値になる。
- [x] **Step 3 — 頂点編集**: 頂点ドラッグ移動（面積リアルタイム更新）、辺中点のゴースト頂点ドラッグで頂点追加、右クリック / Delete で頂点削除（3 頂点未満は不可）。
      → verify: 追加・移動・削除の各操作が動き、面積が追従する。
- [x] **Step 4 — 輪郭の自動検出**: 田んぼ内 1 クリック（シードポイント）→ 表示タイルを canvas 化 → OpenCV.js でフラッドフィル + モルフォロジー → `findContours` → `approxPolyDP`（epsilon は周長の 1〜2%）で 4〜15 点程度に間引き → `map.unproject()` で緯度経度化し、Step 3 と同じ編集可能ポリゴンとして表示。
      → verify: 実写真の田んぼで検出 → 頂点数が過剰でない → 手動調整できる。
- [x] **Step 5 — 仕上げ**: 複数田んぼの管理（一覧・選択・削除）、GeoJSON エクスポート / インポート（または localStorage 保存）。
      → verify: 2 枚以上の田んぼを保存・再読み込みできる。

## テスト

Playwright（Chromium）で実際にブラウザを動かす。地図タイルと OpenCV.js を含めて確かめたいので、単体テストは置いていない。

```
npx playwright install chromium   # 初回のみ
npm test        # dev サーバに対して実行
npm run test:dist # build して preview に対して実行
```

`tests/helpers.ts` に共通の操作（描く・ドラッグする・検出する・一覧を読む）をまとめてある。
検出の検証は表示中の画像を読むので、画面サイズと解像度を `playwright.config.ts` で固定している。
`devices[...]` を指定すると viewport を上書きされてシードの位置が変わるので、使っていない。

## 追加した機能

### メジャー（距離計測）

パネルの入力方法に「メジャー」を足した。クリックで点を継ぎ足す折れ線計測で、各辺の長さは中点に、合計はパネルに出す。ダブルクリック / Enter で終了、Esc かボタンで消去。距離は面積と揃えて `@turf/distance`（測地線）で計算する。

- ラベルは MapLibre の symbol レイヤーではなく HTML の Marker で描く。symbol は字を出すのに glyphs の配信元が要り、外部フォントへの依存が増えるため。画面上 36px 未満の辺はラベルが重なるので出さない。
- 線は白い破線に黒い縁取り。航空写真の明るい所と暗い所のどちらでも読めるようにするため。
- メジャーを離れると引いた線は消す。残すと、他のモードには消す手立てがないうえ、**自動検出が写真として読み込んでフラッドフィルの壁になる**（レビュー中に再現。通常 1,192 ㎡ を返す圃場が、線を横切らせると検出失敗になった）。
- レビューで直した点: 田んぼの交差警告がメジャーのヒントを乗っ取っていた／合計を隠しても DOM に古い値が残っていた／`formatDistance` が 999.6m を「1,000 m」と出していた／ラベルの Marker を毎フレーム作り直していた（辺の番号で持ち回す形に変更）／1 点しか置いていないのに「Enter で終了」と案内していた。
- `@types/node` を入れたら `src` にも Node の型が入り、`process` を書いても型検査を通る状態になっていた。`tsconfig.json` は `types: []` で `src` のみ、テストと設定ファイルは `tsconfig.test.json` に分けた。

## リスク

- 自動検出精度は畦道のコントラスト依存。検出結果は常に「編集可能な下書き」として Step 3 に合流させ、手動調整で回収する設計。
  - 地理院タイル（収穫後の裸地）では十日町の小区画圃場を 1 枚も検出できなかった。道路や畦と色が近く、フラッドフィルが止まる境界がないため。市の航空写真（夏期・稲が青い）に替えたことで 6/7 が検出できるようになった。
  - 検出の寸法はピクセルではなく**地上距離**で持つ。画像ソースやズームで 1px の地上距離が数倍変わるため、ピクセル固定だと「解像度を上げたら検出できなくなる」ことが実際に起きた（平滑化 1.2m 相当＝3px では市の写真の条間を消せなかった）。
  - 平滑化 2m は、市の写真（6/7 検出）と地理院タイル（5/5 検出）の折り合いで決めた値。地理院タイルだけの地域では大きめに漏れることがあるが、検出結果は編集可能な下書きなので手で詰める。
- 国土地理院タイルは地域により撮影年次が古い場合あり。必要になった時だけ Mapbox 衛星へ切り替え。

## Review

### Step 1 — 地図表示（完了）

- 構成: `index.html` / `src/main.ts`（起動）/ `src/map.ts`（地図生成）/ `src/style.css`。Vite + vanilla TS、依存は maplibre-gl のみ。
- `src/map.ts` で国土地理院シームレス写真を raster source（`maxzoom: 18`、地図側 `maxZoom: 21` でオーバーズーム）として読み込み、出典を `attribution` に設定。頂点編集の誤操作を避けるため回転・傾斜は無効化。
- 初期表示は新潟市南区の水田地帯 `[139.033, 37.78]` / zoom 17。
- 検証: `npx tsc --noEmit` 通過。`npm run dev` + Playwright（headless Chromium）で読み込み → console エラー 0、タイル 30 件すべて 200、出典表記「国土地理院 全国最新写真（シームレス）」を確認。スクリーンショットで水田の航空写真を目視確認。

### Step 2 — 手動ポリゴン描画（完了）

- `src/draw.ts` に `PolygonDrawer`。GeoJSON ソース 1 つに頂点（Point）・折れ線（LineString）・面（Polygon）を入れ、`geometry-type` フィルタで 3 レイヤーに振り分ける。未閉合中は最後の頂点からカーソルまでラバーバンド表示。
- 閉合は 4 経路: 開始点から 12px 以内のクリック（`map.project()` で判定）、ダブルクリック（`originalEvent.detail >= 2`）、Enter、いずれも 3 頂点以上が条件。Esc で描きかけを破棄。
- `src/units.ts` で ㎡ / 反 / 畝 を換算（1 畝 = 30 坪、1 反 = 10 畝）。面積は `@turf/area`、3 頂点目から暫定値をリアルタイム表示。
- パネル UI は `index.html` + `src/style.css`。
- 途中で見つけた不具合 3 件:
  1. maplibre-gl は worker を「自分の `import.meta.url` の兄弟ファイル」として実行時に解決するため、バンドラが worker を出力できない。dev でも build でも worker のリクエストが失敗し、GeoJSON ソースが永久に未ロード＝描画が一切出ない状態だった。`setWorkerUrl()` に Vite が出力した URL を渡し、`vite.config.ts` は `worker: { format: 'es' }`（maplibre は `new Worker(url, { type: 'module' })` で起動する）だけにした。
  2. `#area { display: grid }` が `hidden` 属性を打ち消し、面積表示が消えなかった → `#area[hidden]` で明示的に打ち消し。
  3. 「新しく描く」ボタンにフォーカスが残ると、閉合したいときの Enter がボタンのクリックに化けて描きかけが消える → クリック後に `blur()`。キーボードで Tab した場合はボタンの操作が正しいので、`keydown` 側でボタンを対象から外す。
- レビュー: `/code-review` + `/simplify` の 4 観点。適用したのは上記 worker 修正のほか、`lngLat.toArray()` の利用、GeoJSON ソースをフィールド保持（`instanceof` ガードの削除）、`DrawState` を `vertexCount` に縮小、mousemove の再描画を rAF で 1 フレームに間引き、面積は頂点が変わったときだけ再計算、カーソル書き込みの重複回避、2 頂点で開始点をクリックしたときの重複頂点の防止、`@types/geojson` の明示的な devDependency 化、ボタンの初期 `disabled`。見送り: `#closed` → `#mode` 化と頂点への `index` 付与（Step 3 で必要になった時点で入れる）、`destroy()`（インスタンスは 1 つだけ）。
- 検証: `npx tsc --noEmit` / `npm run build` 通過。Playwright の smoke テスト 10 項目が dev / production build の両方で全 PASS（初期状態、1 頂点、2 頂点での開始点クリック無視、暫定面積、スナップ閉合、Enter 閉合、reset 直後の Enter 閉合、フォーカス時 Enter のやり直し、ダブルクリック閉合、Esc 破棄）、console / network エラー 0。100px 四方の正方形で 2,223 ㎡（zoom 17 / 緯度 37.78 の理論値 2,226 ㎡ と 0.1% 一致）。

### Step 3 — 頂点編集（完了）

- `PolygonDrawer` を `#closed: boolean` から `#mode: 'drawing' | 'editing'` に変更。閉合後は編集モードに入り、頂点ドラッグ、辺の中点ゴーストからの頂点追加、右クリック / 選択 + Delete での削除ができる。面積はドラッグ中も追従する。
- 当たり判定は `queryRenderedFeatures` をやめ、`map.project()` による画面座標の距離判定に変更。`setData` は worker 経由で非同期に反映されるため、描画済みフレームを見る `queryRenderedFeatures` は連続操作で古い頂点番号を返し、別の頂点を消してしまう。あわせて頂点をゴーストより優先し、重なっても頂点を掴めるようにした。
- 中点ゴーストは辺がスクリーン上 32px 未満なら出さない（ズームアウト時に頂点と重なり、掴んだつもりが頂点追加になるため）。ズームで条件が変わるので `zoomend` で引き直す。
- 交差した輪郭（bowtie）を検出して警告する。turf は交差したリングでも打ち消し合った値を黙って返すので、面積だけが成果物のこのツールでは黙って間違った数値を出すのが最悪の失敗になる。
- 実装中に見つけた不具合: MapLibre は右ボタンの `mousedown` で `preventDefault()` すると後続の `contextmenu` を握り潰す。ドラッグ開始を主ボタンのみに限定して解決（macOS の ctrl + クリックも同様に除外）。
- レビューで見つけて直した点: 地図の外（パネル上・ウィンドウ外）でボタンを離すとドラッグが終わらず、頂点がカーソルに追従し続けた → `mouseup` を `window` で待ち、`buttons === 0` の mousemove でも終了する。ドラッグ中の削除で `#dragging` の番号がずれる → ドラッグ中は削除しない。`MIN_VERTICES` の `3` が `main.ts` に複製されていた → `DrawState.canDeleteVertex` に集約。ほかに role 判定の三項演算子、`#edgeMidpoint` の重複、paint 式の `case` / `match` 混在を整理。
- 見送り: 編集中の Esc は選択解除のみ（閉合済みの輪郭を Esc で捨てると作業が消えるため、やり直しは「新しく描く」に一本化）。3 頂点での右クリックはネイティブメニューも出ないまま無反応になるが、パネルに「これ以上は減らせません」と出る以上の手当てはしない。複数ポリゴン対応（Step 5）に向けた source / layer の ID 化は、必要になってから。
- 検証: `npx tsc --noEmit` / `npm run build` 通過。Step 3 の smoke テスト 12 項目と Step 2 の 10 項目が dev / production build の両方で全 PASS、console エラー 0。Step 3 の項目は編集モード遷移、頂点ドラッグでの面積増加、ドラッグ中の面積追従、中点ドラッグでの頂点追加、右クリック削除、Delete キー削除、3 頂点でのヒント切り替え、3 頂点未満への削除拒否、地図外で離したときのドラッグ終了、交差の検出・表示・解消。

### Step 4 — 輪郭の自動検出（完了）

- パネルに「手動 / 自動検出」の切り替えを追加。自動検出モードで田んぼの中を 1 クリックすると、`src/detect.ts` が表示中の写真をフラッドフィルして輪郭を推定し、`PolygonDrawer.load()` に渡す。検出後は手動（編集）へ自動で戻すので、そのまま Step 3 の頂点編集で直せる。
- パイプライン: canvas 読み出し → `cvtColor(RGBA2RGB)` → `floodFill`（MASK_ONLY + FIXED_RANGE、4 近傍、許容差 18）→ モルフォロジー OPEN → CLOSE → `findContours(RETR_EXTERNAL)` → シードを含む輪郭を選択 → `approxPolyDP`（epsilon を周長の 1% から上げて 15 頂点以下に収める）→ `unproject`。
- OpenCV.js（13MB）は最初に自動検出モードへ切り替えた時点で読み込む。build では独立したチャンクに分かれ、手動描画しか使わなければ読み込まれない。
- 実装中に見つけた不具合 2 件:
  1. モルフォロジーを CLOSE → OPEN の順にすると、隣の田んぼへ漏れた首が太って切れず、輪郭に細い突起が残る。OPEN → CLOSE に変更。
  2. production build だけが `Promise.prototype.then called on incompatible receiver` で落ちた。opencv.js は CommonJS で `module.exports` に Promise を入れており、CJS → ESM 変換でその `then` が名前付き export になるため、`await import()` が名前空間オブジェクト自体を thenable とみなす。変換を挟まない script タグ読み込みに変更。
- レビュー 3 本（`/code-review high`、検出専用のレビュー、Codex）で見つけて直した点:
  - `contours.get()` は毎回ヒープに Mat を作るのに解放していなかった（検出のたびにリーク）。使わないものはその場で、選んだものは finally で解放する。
  - ローダの Promise が失敗を永久にキャッシュし、一度失敗すると再読み込みまで自動検出が使えなくなっていた。
  - 検出中にモードを往復すると 2 つの検出が重なり、2 回目が青いポリゴンごと写真として読んでいた。通し番号で古い結果を捨て、検出中はモード切り替えを無効にする。
  - タイル待ちと OpenCV 読み込みのあいだに地図を動かされると、シードの画面座標と写真がずれる。撮影はクリック直後に行い、クリック時・撮影時・`unproject` 時のカメラ（中心・ズーム・canvas サイズ）を突き合わせて、ずれていれば失敗として知らせる。
  - モルフォロジーのカーネルが device pixel 固定だったため、Retina では実効サイズが半分になっていた。CSS ピクセル基準にして `scale` 倍する。
  - シードを含む輪郭がないとき最大の輪郭で代用していた。ユーザが指していない田んぼの面積を、それと分からないまま出すことになるので廃止。
  - 端のクリックでシードが画像の外を指すと OpenCV が数値の例外を投げるだけだった。座標を丸めて範囲に収め、数値例外は `exceptionFromPtr` で読めるメッセージに変換する。
  - 地図の読み込み前からパネルを操作できてしまい、自動検出を選んでも手動のままだった。読み込みが済むまでコントロールを無効にする。
- 検証: `npx tsc --noEmit` / `npm run build` 通過。Step 4 の 11 項目、Step 3 の 12 項目、Step 2 の 10 項目が dev / production build の両方で全 PASS、console エラー 0。Step 4 は devicePixelRatio 1 と 2 の両方で実行し、同じ田んぼで 10,119 ㎡ と 10,144 ㎡（差 0.25%）と、解像度によらず一致することを確認。実写真の田んぼを 5 頂点で抽出、初回 2.0〜2.4 秒（13MB の読み込み込み）、以降 0.3〜1.0 秒。6 回連続の検出もすべて成功。

### Step 5 — 複数管理と入出力（完了）

- `PolygonDrawer` は 1 枚を編集する役のまま据え置き、複数枚の保管・描画・入出力を `src/paddies.ts`（モデル・保存・GeoJSON 変換）と `src/paddyLayer.ts`（編集中でない田んぼの描画）に分けた。編集中は青、保存済みは橙。
- 輪郭を閉じると一覧へ自動保存し、localStorage に書き出す（ドラッグ中は 400ms のデバウンス、タブを隠す / 閉じるときは即時に書き出す）。一覧から選ぶとその田んぼへ地図が寄って編集モードに入り、✕ で削除（確認あり）。GeoJSON の書き出しと読み込みに対応。
- テスト作成中に見つけた不具合: 取り込んだ GeoJSON の id をそのまま使っていたため、同じファイルを 2 回読むと id が衝突し、1 枚を削除したつもりで両方消えていた。衝突分は振り直す。
- レビュー 2 本で見つけて直した点:
  - 自動検出の撮影で保存済みレイヤーが隠れておらず、橙の塗りと線がフラッドフィルの壁になっていた。Step 4 で直した「描いたものを写真として読む」不具合が、色違いで再発していた形。`SavedPaddyLayer.setVisible()` を足して一緒に隠す。
  - 交差した輪郭が警告なしで保存・一覧表示・書き出しされていた。Step 3 で「黙って間違った数値を出すのが最悪」として警告を入れたのに、複数管理では抜けていた。一覧は「輪郭が交差」と出し、書き出しは `areaSquareMeters: null` + `selfIntersecting: true` にする。
  - 頂点を動かしても一覧の面積が古いままだった。一覧を作り直さず、編集中の行の面積セルだけを書き換える。
  - 検出中に一覧の選択・削除・読み込みができ、あとから完了した検出が選択を上書きしていた。通し番号を進めて古い検出を捨て、検出中は一覧も無効にする。
  - `localStorage` の参照・書き込みが例外を投げると（Cookie 全面ブロック、容量超過）、起動できなくなる / 削除が途中で止まる。どちらも捕まえて、保存失敗はパネルに出す。
  - `crypto.randomUUID` は secure context 限定で、`vite --host` の LAN IP（http）では例外になる。`getRandomValues` に落とす。
  - 書き出す GeoJSON の外周リングが RFC 7946 の反時計回りになっていなかった。MultiPolygon の読み込みにも対応（QGIS の書き出しは MultiPolygon になることが多い）。
  - 描画のたびに全田んぼの面積を計算して GeoJSON 化していた。地図用はジオメトリだけの変換に分け、保存済みの集合が変わったときだけ差し替える。
- 検証: `npx tsc --noEmit` / `npm run build` 通過。Step 5 の 31 項目、Step 4 の 11 項目、Step 3 の 12 項目、Step 2 の 10 項目が dev / production build の両方で全 PASS、console エラー 0。Step 5 は保存・再読み込み後の復元、一覧からの選択と地図の追従、頂点編集への面積追従、削除と確認のキャンセル、GeoJSON の往復（リングの閉じ・巻き方向・名前・面積）、同一ファイルの二重読み込み、MultiPolygon、壊れた JSON / Polygon を含まないファイルのエラー表示を含む。

## Step 6 — スタンドアロン版とオフライン対応（作業中）

- [x] 単一 HTML を吐くビルドを用意する（`npm run build:standalone`）。→ verify: `file://` で開いて全テスト通過。
- [x] `file://` で描いた線が見えない不具合を直す。原因は module worker で、`file://` では blob でも起動できず、GeoJSON レイヤだけ処理されない。→ verify: 実 Chrome の `file://` でポリゴンが見える。
- [x] 「描いたものが実際に地図に出ている」ことを見るテストを足す。数値だけ見ていたので今回の不具合を素通りさせた。→ verify: 直す前の単一 HTML で 0px となり落ちる、直したあと通る。
- [x] `file://` で IndexedDB が使えるか確かめる。→ verify: Chrome は 10GB、WebKit は 1GB。ただし Blob は入らないので ArrayBuffer で持つ。
- [x] タイルを IndexedDB にためて、オフラインではそこから返す。→ verify: 通信を切って開き直しても写真が出て、取りに行った要求は 0 本。
- [x] 表示中の範囲を先に落としておく操作と、ためた量の表示・削除。→ verify: 保存 → オフライン → 表示 → 消す、の一通り。

### 経過

- 単一 HTML で線が出なかった原因は module worker。`file://` では blob 越しでも起動できず、`new Worker` が例外ではなく非同期の error で死ぬため、maplibre のフォールバックにも引っかからない。GeoJSON の処理だけが worker 側にあるので、写真は出るのに描いたものだけ出ない、という形になっていた。単一 HTML では worker を iife で出し、その blob URL の末尾に `#.cjs` を足して maplibre の classic worker 経路に乗せている。
- テストが数値しか見ておらず、この壊れ方を素通りさせていた。地図の canvas を読んで、描いた色の画素数を数える確認を足した（直す前の単一 HTML では 0px で落ちる）。
- オフラインは IndexedDB。`file://` でも Chrome は 10GB、Safari は 1GB 使える。ただし Safari は Blob を入れられないので ArrayBuffer で持つ。

### レビューで見つけて直した点

- 保存するズームが 1 段ずれていた。maplibre は 512px 基準で数えるので、256px のタイルでは地図の z17 に対して z18 を要求する。地図のズームでためていたため、現地では常に 1 段粗い引き伸ばしになっていた。「オフラインで 1 本も取りに行かない」ことをテストで見るようにして、この種のズレを画ではなく要求で捕まえる。
- ためられなかったのに「保存しました」と出していた。保存領域が一杯のときと IndexedDB を開けないときは黙って握っていたので、枚数を出して伝える。
- 見ているだけでもタイルはたまるのに、たまった量は起動時にしか数えていなかった。数え直しが安くなったので、地図が落ち着くたびに出し直す。
- 枚数と合計サイズは全件を読んで数えていた。控えを 1 レコード持ち、メモリにも載せる。
- 単一 HTML でグローバルの `Worker` を差し替えていた。maplibre は URL が `.cjs` で終わるときだけ classic worker を使うので、blob URL の末尾にフラグメントを足すだけで済むようにした。
- オフライン欄を足したことでパネルが横に広がり、その下の地図へのクリックが届かなくなっていた（頂点が黙って落ちる）。`max-width` に padding を含めて上限内に収め、田んぼを保存したあとの幅もテストで見る。

## Step 7 — UI の刷新と GitHub Pages 化（作業中）

- [x] リポジトリを `~/Developer/GitHub/yoshi2ys/nakasato-tanbo-map` へ移し、public で公開する。→ verify: https://yoshi2ys.github.io/nakasato-tanbo-map/ が実ブラウザで動く。
- [x] 単一 HTML をやめ、`base` をサブパスに変えて Service Worker を入れる。→ verify: 通信を切って開き直しても地図が出る（build 済みモード）。
- [x] 田んぼ・計測・ピンを 1 つのモデルにまとめ、名前・色・表示の有無を持たせる。→ verify: 保存と書き出しが同じ GeoJSON のまま、旧いファイルも読める。
- [x] 編集器を面・線・点に広げる。→ verify: 既存の頂点編集のテストがそのまま通る。
- [x] サイドバー・ツールバー・インスペクタ・設定画面。→ verify: パネルが地図を覆わない（列で分ける）。
- [x] ピンのアイコン（Material Symbols の path を同梱）。→ verify: 追加の読み込みが 1 本も要らない。
- [x] 自動検出の下見（カーソル静止 200ms、ROI 512px）。→ verify: 検出 27〜48ms、初回の撮影のみ 329ms。
- [x] 重ねる地図（標準地図・淡色地図・陰影起伏）と設定画面。→ verify: 出したものだけがオフライン保存の対象になる。
- [ ] 3 モードでの通し確認と、実機での目視。

### この段階で分かったこと

- `style.load` の前にソースやレイヤーは足せない。地図まわりの組み立ては全部そこから始める。編集器は作られた時点で状態を知らせてくるので、組み立て中は描画しない印が要る。
- パネルの開け閉めで地図の幅が変わると、同じ場所を指したつもりのクリックが別の場所に落ちる。右の列は選択の有無にかかわらず空けておく。
- リングの巻き方向の判定式は 2 通りあり、符号が逆になる。移すときに取り違えて、書き出した GeoJSON が時計回りになった（テストが捕まえた）。
- 筆界（登記所備付地図）は地理院にも法務省にも XYZ 配信がないので、重ねる地図には入れられない。

### 整理で残した宿題

- `itemLayer` `preview` `editor` の 3 つが、GeoJSON のソースとレイヤーの世話（追加・可視・後始末）を似た形で書いている。共通の土台に寄せられるが、`editor` は実地の修正が詰まったファイルなので、触るときは単独で。
- `pins` と `measureLabels` の Marker 差分更新も同じ形。ただし鍵が id と辺番号で違い、無理に寄せると読みにくくなる。
- 編集器の `point` は「置く」だけを持ち、動かすのは `PinLayer` にある。1 つの形を 2 か所で見ている状態なので、`point` を編集器から外して `PinLayer` に寄せる余地がある。

## Step 8 — ツールバーの記号と、電話での操作（完了）

- [x] 道具に記号を付け、名前をその下に置く。→ verify: 記号と名前の両方が出ていることをモバイルのテストで見る。
- [x] 画面が狭いときは列をやめ、一覧とインスペクタを下から出すシートにする。→ verify: iPhone 14 Pro の幅でボタンから開閉できる。
- [x] 指で頂点を動かせるようにする。→ verify: CDP で本物のタッチを送り、面積が変わることを見る。
- [x] タブレットの幅では列を細くして、地図の取り分を残す。→ verify: iPad 横で地図が 580px 以上。

### 分かったこと

- maplibre はタップを click に直してくれるので、頂点を「置く」のは元の経路で動く。動かすほうは touch のまま来るので、`touchstart` / `touchmove` を別に取る必要がある。取らないと地図のパンに持っていかれる。
- 掴める距離は指では 9px では足りない。20px に広げた（矢印のときは 9px のまま）。
- テストで三角形の頂点を縦に動かしても、底辺と高さが変わらない向きだと面積が 1 桁も動かない。動かす向きは意図して選ぶ。

# 2026-08-18 — 情報パネル・一覧の絞り込み・表示モードの回転

右のインスペクタは今の情報量に対して大きすぎる。地図の右上に小さな情報パネルを出し、
名前・色・アイコン・削除は「詳細」シートに移す。書き出しと読み込みは設定に集約する。

## 決めたこと

- パネルは表示専用（種別・名前・面積/長さ・座標）。編集操作は「詳細」シートと「編集」ボタン。
- 狭い画面（820px 以下）は今のボトムシートのまま。右上パネルは電話だと地図の大半を覆う。
- 回転は表示モードだけ許す。編集に入るときは北へ戻す。傾き（pitch）は切ったまま。
- 部品の寸法は PC では今のまま。狭い画面（820px 以下）だけ HIG に合わせて一回り大きくする。

## Plan

- [x] 書き出し・読み込みを設定シートへ移す。`#library-actions` を畳み、設定に「データ」の節を作る。
      id（`#export` / `#import` / `#import-file`）は変えないので app.ts の配線はそのまま。
      → verify: `library.spec.ts` が設定を開いてから押す形で通る。設定の注記の文言も直す。
- [x] 一覧の歯車の位置。`#sidebar-header` の `space-between` が原因で、h1 と閉じるの間に浮く。
      h1 に `margin-right: auto` を与えて右に寄せる。
      → verify: スマホ幅で歯車と閉じるが右端に並ぶ（mobile.spec に位置の確認を足す）。
- [x] 一覧に検索と種別フィルタ。Sidebar が語と種別を持ち、`render` で絞る。
      → verify: 新規 `filter.spec.ts` — 語で絞る、種別で絞る、該当なしの表示、選択は絞っても壊れない。
- [x] インスペクタ → 右上パネル + 詳細シート。`ui/panel.ts` と `ui/detailSheet.ts` を作り
      `ui/inspector.ts` を消す。`#app` は 2 列（一覧・地図）になる。
      ズームは右上をパネルに譲って右下へ移す。
      → verify: `mode.spec` / `items.spec` / `pin.spec` / `mobile.spec` を新しい id で通す。
- [x] Playwright の viewport を 1740 → 1460 に。右 280px の列が無くなるので、
      これを直さないと地図の幅が変わり `PADDY_SEEDS` が別の場所を指す。
      → verify: `detect.spec` が今までと同じ検出率で通る。
- [x] 表示モードだけ回転。`map.ts` に切り替えを足し、`setMode` から呼ぶ。編集に入るとき bearing を 0 へ。
      `detect.ts` の `cameraKey` に bearing を足す。bearing≠0 のときだけ「北に戻す」を出す。
      → verify: 新規 `rotate.spec.ts` — 表示で回る、編集では回らない、編集に入ると北へ戻る。
- [x] スマホの寸法を HIG に合わせる。触れるものは 44px 角を下回らせない（一覧の歯車 26px、
      閉じる 30px、一覧の表示切り替えと削除、色とアイコンの選択、モードとツールのラベル）。
      字も一段上げる。触るのは `@media (max-width: 820px)` の中だけで、PC 側は変えない。
      → verify: `mobile.spec` に主要なボタンの実寸（44px 以上）を確かめる検査を足す。

## Review

- インスペクタの列をやめ、地図の右上に情報パネル（`src/ui/panel.ts`）を出した。名前・色・
  アイコン・削除は詳細シート（`src/ui/detailSheet.ts`）へ。`src/ui/inspector.ts` は削除。
- 書き出し・読み込みは設定シートの「データ」へ。id は据え置きなので app.ts の配線は変えていない。
- 一覧に検索と種別フィルタ（`src/ui/sidebar.ts`）。新しく作ったときと読み込んだときは
  絞り込みを外す（描いたものが一覧に出ないと、保存できたのか読めない）。
- 一覧ヘッダの歯車は右寄せ（h1 に `flex: 1`）。狭い画面の当たり判定は 44px 以上に統一。
- 回転は表示モードだけ。編集に入るとき北へ戻す。傾きは `maxPitch: 0` でどの経路からも塞いだ
  （キーボードの Shift+↑ が抜けていた）。`cameraKey` に bearing を追加。
- ズームとコンパスは右上から左下へ。右下に置くと出典の帯を押し上げ、その帯が地図のクリックを
  広く奪った（`mode.spec` の選択解除が落ちて気づいた）。
- レビューで見つけて直したもの: 編集中もキーボードで回転・傾斜できた／新しく描き始めても前の
  選択が残りパネルが下書きに切り替わらなかった／スマホでパネルが一覧と設定の入口を覆っていた／
  `#reset-bearing` が 42px／スマホのトップバーが 393px に収まらず横スクロールしていた。

### 確かめたこと

- `npm run type-check` → エラーなし。
- `npx playwright test` → 93 passed / 1 skipped（9.2 分）。skip は dist でしか走らないオフラインの 1 件。
- `npm run test:dist` → 94 passed（8.5 分）。オフラインの 2 件も、設定に移した DOM で通る。
- 新しいテスト: `tests/filter.spec.ts`（絞り込み 5 件）、`tests/rotate.spec.ts`（向き 6 件）、
  `tests/mobile.spec.ts` に 44px の確認 2 件。`tests/draw.spec.ts` の古いパネル位置の検査は
  新しいパネル向けに書き直した（以前は要素が無く、素通りしていた）。

### 残していること

- スマホでは描きかけの面積を出さない（パネルが下から出ると「確定」を押し上げてしまうため）。
  従来のインスペクタも同じで、画面が広いときだけ下書きの数値が出る。
- パネルが出ているあいだ、出典の帯と縮尺はその裏に隠れる（従来のインスペクタも同じ）。

# 2026-08-18 — 使ってみて出た直し

## やったこと

- パネルの「編集」が白地に白文字だった。`#panel-actions button`（id + 要素）のほうが
  `#panel-edit`（id だけ）より詳細度が高く、背景の白が勝っていた。選択子を揃えて直した。
- 広い画面の編集中は、名前・色・アイコンの欄をパネルの中に出す。欄は 1 組しか作らず、
  置き場所（パネル / 詳細シート）を移す。狭い画面と表示モードは今までどおりシート。
- メジャー: 引いている最中も辺の長さを線の上に出す。線は破線にして、田んぼの輪郭と見分ける。
  確定したあと、線から離れたところを押せば次の 1 本を引ける（道具を押し直さなくてよい）。
  表示モードでは、選んでいなくても出ている計測すべてに長さを出す。
- GitHub Actions を Node 24 で動く版に上げた（checkout v7 / setup-node v5 /
  upload-pages-artifact v5 / deploy-pages v5）。使っている入出力は変わっていない。

## 確かめたこと

- `npm run type-check` → エラーなし。
- `npx playwright test` → 96 passed / 1 failed（パネルの寸法の検査。編集中は欄のぶん
  248px に広がるので、検査の値を実態に合わせて修正 → 通過）。1 skipped は dist 専用。
- 新しいテスト: 欄の置き場所（広い画面 / 狭い画面）、引いている最中のラベル、連続で引ける、
  表示モードのラベル。`measure.spec` の「選んでいない計測のラベルは出さない」は方針が
  変わったので書き換えた。

## 進め方の変更

全件は 9 分かかる（ワーカー 1 本＋実タイル取得）。触った範囲の spec だけ回し、
全件はコミット直前に 1 回だけにする。さらに縮めるなら、タイルを HAR に録って
`page.routeFromHAR` で再生し、ワーカーを増やす手がある。

# 2026-08-18 — 現地の見え方の直し

- 計測の破線は、縁取り（黒）も同じ間隔で切る。片方だけ実線だと黒い線が通って破線に見えない。
  `dasharray` の単位は線の太さなので、線（2.5px）と縁取り（6px）で値を割り当て直した。
  選択の強調は太さではなく縁取りの濃さ（片方だけ太さを変えると間隔まで変わる）。
- 白に近い色の記号は、白い地に沈む。色は変えず、暗い縁（drop-shadow）だけを足す。
  一覧・カードの見出し・地図のピンに効かせる。判定は輝度 0.8 超（既定の橙は 0.71 で対象外）。
- 確定した線に点を継ぎ足せるようにした（「点を継ぎ足す」）。クリックの意味は
  「線から離れる＝次の 1 本」に使ってしまったので、継ぎ足しはボタンで受ける。面には出さない。
- 併せて古い不具合を 1 つ。選んだものを編集器に載せるとき一覧を描き直しておらず、
  ドラッグや継ぎ足しのあいだ一覧の行の数値だけ古いまま止まっていた（保存とカードは正しい値）。

## 確かめたこと

- `npm run type-check` → エラーなし。
- 触った範囲: `measure` 13 件、`items` / `edit` / `mobile` 27 件 → すべて通過。
- 全件はコミット直前に 1 回（下記）。

# 2026-08-18 — 継ぎ足しを手数なしにする

- 計測を選んで編集に入ったら、そのまま末尾から継ぎ足せる状態にする（たいてい続きを測りたい）。
  「点を継ぎ足す」ボタンは、その場で確定したあと再開したいときのために残す。
- 継ぎ足し中でも、置いた点を掴んで動かせる。これまでドラッグは確定後だけだった。
  面は開始点をクリックして閉じる操作と取り合いになるので、線のときだけ許す。
- 継ぎ足し中の「やめる」は、継ぎ足したぶんだけを捨てて元の線に戻す。
  以前はすべて捨てていたので、保存済みの線が地図から消えたように見えた。
- 途中で 1 つ壊して直した。「掴んで離した直後のクリックは無視する」を、動かしていない場合にも
  適用してしまい、ダブルクリックの確定が効かなくなっていた。実際に動いたときだけ無視する。

## 確かめたこと

- `npm run type-check` → エラーなし。
- 触った範囲: `measure` 15 件、`draw` / `edit` / `mode` 24 件、`mobile` / `pin` 18 件 → すべて通過。

# 2026-08-18 — フォルダと画像の書き出し

## 決めたこと

- フォルダは 1 段だけ。item が「どのフォルダか」を 1 つ持つ（入れ子もタグもなし）。
- 一覧は登録順をやめて名前順。数字は自然順（`田んぼ 2` が `田んぼ 10` より前）。
- 画像は、選んだ比率の枠を画面に出し、地図を動かして合わせる。枠の大きさはスライダ。
- 書き出す画像にはピンと距離のラベルも入れる。見えているとおりの絵にする。

## Plan — フォルダと名前順

- [x] `items.ts` に `folder?: string` を足す。GeoJSON の properties に出し、読み込みでも拾う。
      未設定は「未分類」として扱う（保存する値は持たない）。
      → verify: 書き出し → 読み込みでフォルダが往復する。旧いファイルもそのまま読める。
- [x] 並びを名前順にする。`Intl.Collator('ja', { numeric: true })` で自然順に。
      → verify: `田んぼ 2` が `田んぼ 10` より前に出る。
- [x] 一覧をフォルダごとに区切る。見出しを押すと折りたたむ。「未分類」は最後。
      折りたたみの状態は設定に持つ（`tanbo-map.settings`）。
      → verify: 折りたたみが再読み込みしても残る。絞り込みと重ねても壊れない。
- [x] フォルダの付け替えは詳細の欄で。既存の名前から選ぶか、新しい名前を打つ（datalist）。
      → verify: 新しい名前を打つとフォルダができ、空にすると未分類へ戻る。

## Plan — 画像の書き出し

- [x] 表示モードに「画像を書き出す」を置く。形式（PNG / JPEG）と比率（1:1 / 4:3 / 3:4）を選ぶ。
      → verify: 選んだ比率の枠が画面に出て、地図を動かすと中身が変わる。
- [x] 枠の中を canvas から切り出す。回転はそのまま写る（画面の canvas を読むため）。
      → verify: 回した状態で書き出すと、回った絵で出てくる。
- [x] ピン・距離のラベル・出典を 2D で描き込む。HTML の Marker は canvas に写らないため。
      JPEG は透明が黒くなるので、先に白で塗る。
      → verify: ピンとラベルが画像に入る。出典の字が隅に入る。
- [x] ダウンロードする。名前は日付と時刻から作る。
      → verify: 実際に PNG と JPEG が落ちてきて、比率が選んだとおり。

## Review

- フォルダは `Item.folder`。未設定は「未分類」で、フォルダを 1 つも使っていないうちは見出しごと出さない
  （1 つしかない括りに名前を付けても読む先が増えるだけ）。畳んだフォルダは設定に残す。
- 並びは名前順。`Intl.Collator('ja', { numeric: true })` なので `田んぼ 2` が `田んぼ 10` より前に来る。
  ja の照合ではカタカナが漢字より前に来るので、既定の名前だと「ピン → 計測 → 田んぼ」の順になる。
- 画像は `snapshot.ts`。canvas を枠の形に切り、ピン・ラベル・出典を 2D で描き足す。
  回転は canvas をそのまま切るので自動的に写る。JPEG は透明が黒くなるので先に白で塗る。
- 一覧の行は `li.item-row`（フォルダの見出しと同じ `li` で拾わないように）。テストの指し方も揃えた。

### 確かめたこと

- `npm run type-check` → エラーなし。
- `folder` 6 件、`image` 7 件、`filter` / `items` / `library` / `measure` / `pin` → 通過。
- 全件はコミット直前に 1 回。

